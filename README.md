# This is a repo for practicing different features of Airflow.

#### To remove example dags
```
docker-compose down -v
```
#### To initialize airflow
```
docker-compose up airflow-init
```
#### To run airflow in detached mode
```
docker-compose up -d
```
